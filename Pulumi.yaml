name: pulumi-infra
description: A minimal AWS TypeScript Pulumi program
runtime: nodejs
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-central-1
    arti_domain:
      default: artithewriter.com